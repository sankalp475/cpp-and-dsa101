#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {

	// pair<int, pair<int, pair<int, int>>> p = {1, {2, {3, 4}}};

	// cout<<p.first<<endl;
	// cout<<p.second.first<<endl;
	// cout<<p.second.second.first<<endl;

	// pair<pair<int, int>, pair<int, int>> p = {{2,5}, {6,9}};

	// cout << p.first.first << endl;
	// cout << p.first.second << endl;
	// cout << p.second.first << endl;
	// cout << p.second.second << endl;

    // pair<int, int> a[] = {{2,5}, {7, 9}, {4, 1}};
	// cout << a[0].first << " " << a[1].second << endl;


    // vector<int> v;
	// v.push_back(1);
	// v.push_back(2);
	// v.emplace_back(12);
// 	// cout << v[2] << endl;

// 	vector<pair<int, int>> vec;
// 	vec.push_back({1, 3});
// 	vec.emplace_back(4, 16);

	// cout << vec[1].second << endl;

	vector<int> v[5];
	// cout << v << endl;

}